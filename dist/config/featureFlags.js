// Feature flag configuration for ABDStyles
export const featureFlags = {
    // Enables live telemetry mode globally. Set to false to disable across all apps.
    liveModeEnabled: true,
};
//# sourceMappingURL=featureFlags.js.map